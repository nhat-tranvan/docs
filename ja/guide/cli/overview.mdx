---
title: CLI
description: "CloudThinker CLI をインストールし、ターミナルからログインして、任意のシェル・スクリプト・CI パイプラインでエージェントを実行します"
icon: terminal
---

<Note>
**プレビュー** — CloudThinker CLI は早期アクセス段階です。コマンド名、フラグ、設定フォーマットは一般提供前に変更される場合があります。プレビューへの参加は[こちらからお問い合わせ](https://cloudthinker.io/contact)ください。
</Note>

`cloudthinker` はワークスペースをターミナルに持ち込みます。CloudThinker のコーディングエージェントを自分のマシンで実行し、任意のスクリプトから Anna にプロンプトを送れます。バイナリ 1 つだけで、追加のランタイムは不要です。

## できること

| 機能 | 説明 |
| --- | --- |
| コーディングエージェントの実行 | `cloudthinker` はカレントディレクトリでエージェントセッションを開始します。ファイル編集とコマンド実行はローカル、モデルと[コネクション](/ja/guide/connections/overview)はクラウド側のままです |
| ヘッドレスプロンプトの送信 | `cloudthinker chat -p "..."` は Anna にプロンプトを 1 つ送り、回答を stdout に出力します。そのままパイプできます |
| 長時間の実行を追跡 | 待たずに送信し、最近の実行を一覧し、あとから別のマシンで回答を回収できます |
| レビューの確認 | マージリクエストやプルリクエストに対する [Review](/ja/guide/code-review/overview) のステータス、判定、指摘を読み取ります |
| スクリプトの認証 | 独自の認証情報が必要なツールに、短命のベアラートークンを渡せます |

## インストール

```bash
curl -fsSL https://cloudthinker.ai/install.sh | sh
```

インストーラーはプラットフォームを判別し、`cloudthinker` バイナリを `~/.local/bin` に配置して `PATH` に追加します。新しいシェルを開いて確認してください。

```bash
cloudthinker --version
```

対応環境は macOS（Apple Silicon および Intel）と Linux（x86_64 および arm64）です。更新はいつでも `cloudthinker update` で行えます。

## 最初のセッション

<Steps>
  <Step title="ブラウザーでログインする">
    ```bash
    cloudthinker login
    ```

    CLI が同意ページの URL を表示し、ブラウザーで開きます。リクエストを承認し、アカウントが複数のワークスペースを持つ場合は 1 つ選ぶと、ターミナルが保存したワークスペースを表示します。

    <Frame>
      <img src="/images/cli/login-consent-url.png" alt="同意ページの URL を表示する cloudthinker login" />
    </Frame>
  </Step>

  <Step title="自分の資格情報を確認する">
    ```bash
    cloudthinker whoami
    ```

    **成功状態:** ホスト、メールアドレス、有効なワークスペースが 1 行で表示されます。
  </Step>

  <Step title="エージェントを動かす、または Anna に尋ねる">
    ```bash
    cloudthinker                                   # このディレクトリでエージェントセッション
    cloudthinker chat -p "Check production health" # ヘッドレスプロンプト 1 回
    ```

    引数なしのコマンドは[ローカルコーディングエージェント](/ja/guide/cli/agent)を起動します。初回実行時に、CLI のバージョンに対応するエージェントビルドをダウンロードします。
  </Step>
</Steps>

## コマンド一覧

| コマンド | 目的 |
| --- | --- |
| `cloudthinker` | カレントディレクトリでローカルコーディングエージェントを実行 |
| `cloudthinker login` | ブラウザー、またはデバイスコードでログイン |
| `cloudthinker logout` | 1 つのワークスペース、またはすべてのワークスペースの認証情報を削除 |
| `cloudthinker whoami` | 保存された認証情報に対応する実際のアカウントとワークスペースを表示 |
| `cloudthinker auth token` | ベアラーが必要なツール向けに現在のアクセストークンを出力 |
| `cloudthinker chat` | ヘッドレスプロンプトの送信、実行の確認、最近の実行の一覧 |
| `cloudthinker review` | マージリクエスト URL から、追跡中のレビューを読み取り |
| `cloudthinker agent` | ローカルコーディングエージェントを実行し、引数をそのまま引き渡し |
| `cloudthinker update` | 最新リリースをインストール |

<Frame>
  <img src="/images/cli/command-surface.png" alt="すべてのコマンドを列挙する cloudthinker --help" />
</Frame>

## 次のステップ

<CardGroup cols={2}>
  <Card title="認証" icon="key" href="/ja/guide/cli/authentication">
    ブラウザーログイン、複数ワークスペース、CI 用トークン
  </Card>
  <Card title="ローカルコーディングエージェント" icon="robot" href="/ja/guide/cli/agent">
    自分のリポジトリでエージェントセッションを実行
  </Card>
  <Card title="ヘッドレスチャット" icon="terminal" href="/ja/guide/cli/headless-chat">
    Anna の回答をスクリプトやパイプラインに流し込む
  </Card>
  <Card title="リファレンス" icon="book" href="/ja/guide/cli/reference">
    すべてのコマンド、フラグ、変数、終了コード
  </Card>
</CardGroup>
