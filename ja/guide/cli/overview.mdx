---
title: CLI
description: "ターミナルから CloudThinker エージェントとの会話、リソースの照会、運用管理を直接実行できます"
icon: terminal
---

<Note>
**プレビュー** — CloudThinker CLI は早期アクセス段階です。コマンド名、フラグ、設定フォーマットは一般提供前に変更される場合があります。プレビューへの参加は[こちらからお問い合わせ](https://cloudthinker.io/contact)ください。
</Note>

`cloudthinker` CLI は、エージェント会話、リソース照会、運用管理をターミナルに集約します。シェルスクリプト、CI パイプライン、コンソールよりもキーボードを好むエンジニアに最適です。

---

## できること

| 機能 | 説明 |
| --- | --- |
| エージェントとのチャット | ターミナルから Alex、Oliver、Tony、Kai、Anna とインタラクティブなセッションを開く |
| ワンショットプロンプト | 単一の `@agent #tool ...` プロンプトを実行して結果を他のツールにパイプする |
| 操作のスクリプト化 | 定期的なクラウド運用タスクをシェルスクリプトでラップして CI で実行する |
| リソース管理 | コネクション、リソース、推奨事項、インシデントを確認する |

---

## インストール

| プラットフォーム | 方法 |
|----------|--------|
| **macOS** | `brew install cloudthinker/tap/cloudthinker` |
| **Linux** | `curl -fsSL https://install.cloudthinker.io \| sh` |
| **Windows** | `winget install CloudThinker.CLI` |
| **Docker** | `docker run cloudthinker/cli` |

---

## 認証

```bash
cloudthinker login
```

CLI は REST API と同じワークスペース [API キー](/ja/guide/api/overview)を使用します。キーは OS のキーチェーンに保存され、平文の設定ファイルには書き込まれません。

---

## コマンド一覧

| コマンド | 用途 |
|---------|---------|
| `cloudthinker chat` | インタラクティブなエージェント会話を開始する |
| `cloudthinker ask "@alex #cost ..."` | ワンショットプロンプト、レスポンスを表示する |
| `cloudthinker resources list` | 統合リソースインベントリを照会する |
| `cloudthinker recommendations list` | アクティブなコスト・セキュリティ・信頼性の推奨事項を一覧表示する |
| `cloudthinker incidents create` | ターミナルからインシデントを開始する |
| `cloudthinker skills run <name>` | [ワークスペースの Skills](/ja/guide/skills/overview) を実行する |
| `cloudthinker connections list` | 設定済みの[コネクション](/ja/guide/connections/overview)を表示する |

---

## 出力フォーマット

| フラグ | フォーマット |
|------|--------|
| `--format text` | デフォルト — ターミナル向けの人が読みやすい形式 |
| `--format json` | `jq`、スクリプト、CI へのパイプ用のマシン可読形式 |
| `--format yaml` | 設定ファイルに親和性の高い出力 |

---

## 関連

<CardGroup cols={2}>
  <Card title="API" icon="code" href="/ja/guide/api/overview">
    HTTPS エンドポイント経由の同等の操作
  </Card>
  <Card title="MCP Server" icon="plug" href="/ja/guide/mcp/overview">
    MCP 対応 AI クライアントから CloudThinker を使用する
  </Card>
  <Card title="タスクとスケジューリング" icon="calendar-check" href="/ja/guide/automation/tasks">
    定期操作のスケジュール設定
  </Card>
  <Card title="Slack 連携" icon="slack" href="/ja/guide/slack-integration">
    Slack から同じ操作を実行する
  </Card>
</CardGroup>
