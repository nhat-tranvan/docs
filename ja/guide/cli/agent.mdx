---
title: Local Coding Agent
description: "自分のリポジトリの中で CloudThinker のエージェントセッションを実行し、ファイル編集はローカル、モデルとコネクションはクラウドに置きます"
icon: robot
---

`cloudthinker agent` は、コードがすでにある場所でコーディングエージェントを実行します。ファイルの読み書きとコマンド実行は自分のマシンで行い、モデル、ワークスペースのメモリー、[コネクション](/ja/guide/connections/overview)は CloudThinker 側に残ります。

## ローカルで動かす理由

- **アップロード不要で自分のリポジトリを扱える。** エージェントは他人のサンドボックス上のコピーではなく、カレントディレクトリで作業します。
- **自分のシェルを使う。** そのマシンにあるツールと認証情報のまま、あなたが打つのと同じコマンドを実行します。
- **同じワークスペース。** セッションの課金、記憶、推論は Web アプリと同じワークスペースで行われます。

## セッションを開始する

<Steps>
  <Step title="リポジトリに移動する">
    ```bash
    cd ~/code/payments-api
    ```
  </Step>

  <Step title="エージェントを起動する">
    ```bash
    cloudthinker
    ```

    引数なしの `cloudthinker` は `cloudthinker agent` と同じです。まずログイン状態を確認し、未ログインならブラウザーログインを提案するため、インストール直後でも 1 コマンドでセッションに入れます。

    **成功状態:** カレントディレクトリで対話セッションが始まります。初回実行時は、対応するエージェントビルドの取得中に `downloading cloudthinker-agent <version>` と表示されます。
  </Step>

  <Step title="やりたい変更を伝える">
    ファイル一覧ではなく、達成したい結果を伝えてください。エージェントが自分でリポジトリを調べ、変更を提案し、確認に必要なコマンドを実行します。
  </Step>
</Steps>

## 単発実行と継続実行

エージェントには独自のオプションがあり、`agent` の後ろの引数はすべてそのまま渡されます。

```bash
cloudthinker agent -p "Add a health check endpoint to the API"   # 非対話で実行して終了
cloudthinker agent -c                                            # 直前のセッションを継続
cloudthinker agent --model <pattern>                             # このセッションのモデルを選択
cloudthinker agent -- --help                                     # エージェント自身のオプション
```

CLI 自身のオプションは `agent` より前に置きます。

```bash
cloudthinker --workspace Production agent
```

エージェントのオプション名が CLI のオプションと重なる場合は `--` を使います。たとえば `cloudthinker agent -- --url http://localhost:3000` のように、`--` 以降はすべてエージェントのものになります。

## エージェントビルドのインストール方法

エージェントは CLI と同じリリースにバンドルとして同梱されており、別途管理するダウンロードではありません。

1. インストールまたは更新後の最初の `cloudthinker agent` が、プラットフォーム向けバンドルを取得します。
2. CLI は展開前に、リリースの SHA-256 サイドカーと照合します。
3. `~/.cloudthinker/agent/bin/<version>/` にインストールし、以降の実行ではそれを再利用します。
4. CLI を更新すると、次回実行時に対応するバンドルが入り、古い版は削除されます。

CLI とエージェントのバージョンは常に一緒に動くため、`cloudthinker update` だけで両方が進みます。

## 対応プラットフォーム

ローカルエージェントは macOS（Apple Silicon および Intel）と Linux（x86_64 および arm64）で動作します。それ以外のプラットフォームでは、別のものにフォールバックせず、対応するエージェントビルドがないことを CLI が報告します。

## トラブルシューティング

<AccordionGroup>
  <Accordion title="毎回ログインを求められる">
    保存済みの認証情報が上書きされています。多くはシェルに残った `CLOUDTHINKER_TOKEN` が原因です。変数を解除して `cloudthinker login` を 1 回実行してください。[認証](/ja/guide/cli/authentication)を参照してください。
  </Accordion>

  <Accordion title="エージェントが別のワークスペースで起動する">
    エージェントは CLI が解決したワークスペースを引き継ぎます。`cloudthinker whoami` で確認し、`cloudthinker --workspace <name> agent` でそのセッションだけ固定できます。
  </Accordion>

  <Accordion title="CLOUDTHINKER_AGENT_BIN に関する警告が出る">
    この変数は、インストール済みのリリースではなくローカルのエージェントビルドを指します。公式バンドルに戻すには変数を解除してください。
  </Accordion>
</AccordionGroup>

## 関連

<CardGroup cols={2}>
  <Card title="ヘッドレスチャット" icon="terminal" href="/ja/guide/cli/headless-chat">
    スクリプトから Anna にプロンプトを 1 つ送る
  </Card>
  <Card title="リファレンス" icon="book" href="/ja/guide/cli/reference">
    すべてのコマンド、フラグ、変数、終了コード
  </Card>
</CardGroup>
