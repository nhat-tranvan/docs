---
title: Workspace Memory
description: "Learn what agents remember between conversations, how memory is shared across your workspace, and how to correct or remove a remembered note"
icon: brain-circuit
---

Workspace memory is the context [agents](/guide/agents) retain between conversations — notes about your environment, your team's preferences, and what happened recently. Agents open each new conversation already carrying it, so you don't re-explain your setup every time.

## Why workspace memory

- **Less repeated context.** An agent that remembers your naming conventions and exempted resources doesn't ask about them twice.
- **Continuity across days.** Each night CloudThinker rolls the day's conversations into a workspace recap, and the next day's conversations start aware of it.
- **A shared team brain.** Memory belongs to the workspace — the shared space your team works in ([glossary](/guide/glossary)) — so what one member teaches an agent benefits everyone.
- **Suggested next steps.** Unfinished action items from recent conversations come back as one-click prompt suggestions in the composer.
- **Nothing is forever.** A memory that goes unused for 90 days is archived and stops informing conversations.

## What agents remember

Agents write memory themselves during normal work — CloudThinker does not record raw transcripts into memory, and there is nothing to configure.

| Memory | What it holds |
|---|---|
| Environment notes | Durable facts agents learn about your infrastructure — conventions, exemptions, recurring context |
| Connection notes | What an agent learned while working with a specific [connection](/guide/connections/overview) |
| People preferences | How each workspace member prefers to be worked with — report formats, tone, priorities you've told an agent about |
| Daily recap | A nightly summary of the workspace's conversations; the last two days are available to every new conversation |

Specialized memory builds on the same foundation: [Incident Memory](/guide/incident/incident-memory) keeps lessons from past investigations, and [Learnings](/guide/code-review/convention-rules) keeps reviewer feedback for Review.

<Info>
Memory is shared across the workspace, not private to you. A preference or fact you tell an agent can inform every teammate's conversations in the same workspace. Keep anything personal or sensitive out of chat, or use a [credential](/guide/credentials) reference instead of pasting secrets.
</Info>

## Prompt suggestions in the composer

When recent conversations left clear next steps, the empty composer shows up to three suggested prompts, each tagged with the conversation it came from. Suggestions draw on roughly the last week of workspace activity, and typing a few characters filters them to match what you're writing.

| Action | What happens |
|---|---|
| Click a suggestion | The prompt is inserted into the composer with a context line pointing at the saved conversation summary — leave that line in place so the agent can read the source |
| Dismiss a suggestion (**x**) | That prompt never appears for you again. Teammates still see it |

## Ask about past conversations

Agents can find and read earlier workspace conversations, so prior work becomes evidence in the current chat.

**Find a conversation.** Ask an agent which earlier chats mentioned a resource, or what the team worked on last Tuesday. Search covers conversation titles, your messages, and visible agent replies in the current workspace — not tool output, agent reasoning, or attachments. Results return short, redacted snippets as leads.

**Read a specific conversation.** Paste a prior CloudThinker chat link or conversation ID and ask the agent to continue from it. The agent reads that conversation's transcript in bounded excerpts and treats it as evidence — it never replays old instructions as commands. Every conversation type works, including assessments, code reviews, pipeline analyses, and Learnings.

Both stay inside the current workspace: agents cannot search or read conversations from another workspace, and deleted conversations are excluded.

## Correct or remove a memory

Memory is agent-maintained, so you fix it the same way it was written — in chat:

1. Tell any agent what's wrong: "Forget that we exempt the staging cluster" or "Update your note — reports go to #finops now, not email."
2. The agent updates or removes the note immediately, and the correction applies to every later conversation in the workspace.

Memories no agent has used for 90 days are archived automatically, so stale context ages out even when nobody corrects it.

## FAQ

<AccordionGroup>
  <Accordion title="Is memory per user or per workspace?">
    Per workspace. Every member's conversations read and write the same memory. Only prompt-suggestion dismissals are personal to you.
  </Accordion>
  <Accordion title="How is memory different from the knowledge base?">
    You upload documents to the [knowledge base](/guide/knowledge) and agents search them on request. Memory is written by agents themselves from what they learn while working, and it reaches them automatically.
  </Accordion>
  <Accordion title="Can an old conversation change what an agent does now?">
    No. Recalled conversations and remembered notes are treated as background facts. Your current instructions always take precedence.
  </Accordion>
</AccordionGroup>

## Related

<CardGroup cols={2}>
  <Card title="Agents" icon="robot" href="/guide/agents">
    Meet the agents that write and use workspace memory
  </Card>
  <Card title="Incident Memory" icon="brain" href="/guide/incident/incident-memory">
    How lessons from past investigations inform later root-cause analysis
  </Card>
  <Card title="Learnings" icon="graduation-cap" href="/guide/code-review/convention-rules">
    Teach Review your team's conventions from reviewer feedback
  </Card>
  <Card title="Knowledge base" icon="book" href="/guide/knowledge">
    Upload the documents you want agents to search on demand
  </Card>
</CardGroup>
