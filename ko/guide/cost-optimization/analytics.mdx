---
title: 비용 Analytics
description: "AWS, GCP, Azure 전반의 클라우드 지출 추세, 귀속, 이상 징후, 예측을 분석합니다"
icon: chart-line
---

비용 Analytics는 클라우드 지출이 어디로 가고 어디로 향하는지 구조적으로 파악할 수 있게 합니다. 자연어로 [Alex](/ko/guide/agents/alex)에게 질문하면 라이브 청구 데이터에서 귀속 분류, 예측, 이상 징후 설명을 얻을 수 있습니다.

비용 신호와 인프라 성능 및 상태 신호를 연계하는 분석은 [인프라 Analytics](/ko/guide/infrastructure/analytics)를 참조하세요.

## 비용 귀속

Alex는 클라우드 지출을 여러 차원에서 동시에 귀속합니다:

| 차원 | 표시 내용 |
|---|---|
| 서비스 | 클라우드 서비스별 지출 — EC2, S3, RDS, Lambda, GKE 등 |
| 리소스 | 인스턴스 또는 볼륨 수준의 비용, API 호출 요금, 데이터 전송 비용 |
| 태그 | 환경, 팀, 프로젝트, 또는 구성한 태그 키별로 그룹화된 비용 |
| 계정 | 다중 계정 조직에서 계정별 합계 및 교차 계정 비교 |
| 제공자 | 자동 통화 변환으로 AWS, GCP, Azure 전반의 정규화된 지출 |

## 이상 징후 감지

Alex는 연결된 청구 스트림을 모니터링하고 비정상적인 지출 패턴을 자동으로 표면화합니다.

| 유형 | 감지 대상 |
|---|---|
| 급증 | 구성된 임계값을 초과하는 일일 지출 증가 |
| 지속적 증가 | 역사적 기준을 초과하는 점진적 상승 추세 |
| 새 서비스 | 이전에 사용하지 않은 서비스의 최초 요금 |
| 리소스 폭발 | 일치하는 배포 이벤트 없이 리소스 수의 급격한 증가 |

[알림](/ko/guide/notifications)에서 경고 임계값과 알림 채널을 구성하세요.

## 예측

Alex는 청구 이력으로부터 비용 예측을 생성합니다. 사용 가능한 예측 기간:

| 기간 | 사용 신호 |
|---|---|
| 월말 | 월간 현재까지 지출에서 외삽된 현재 추세 |
| 분기별 | 이전 분기의 성장률을 더한 계절적 추세 분석 |
| 연간 | 구성된 예산 목표 대비 성장률 조정 예측 |

## 예시 프롬프트

```text
@alex #dashboard current month cloud spending by service
```

```text
@alex #dashboard compare production vs development costs over the last 90 days
@alex #dashboard multi-cloud spending — AWS vs GCP vs Azure this quarter
```

```text
@alex what drove the cost increase last week?
@alex analyze the cost spike on March 3rd and identify the root cause
```

```text
@alex forecast cloud spending for the next quarter
@alex compare projected end-of-month spend against quarterly budget
```

```text
@alex #report export last quarter's costs by service and tag to CSV
@alex #report monthly cost allocation broken down by team tag
```

## 관련 문서

<CardGroup cols={2}>
  <Card title="절감 추적" icon="chart-pie" href="/ko/guide/cost-optimization/savings">
    구현된 추천으로 실현된 절감액을 측정합니다
  </Card>
  <Card title="인프라 Analytics" icon="chart-mixed" href="/ko/guide/infrastructure/analytics">
    비용 신호와 성능, 상태, 활용도를 연계합니다
  </Card>
  <Card title="추천" icon="lightbulb" href="/ko/guide/cost-optimization/recommendations">
    AI가 생성한 추천으로 비용 인사이트를 실행합니다
  </Card>
  <Card title="Artifacts" icon="chart-bar" href="/ko/guide/artifacts/overview">
    대시보드와 리포트를 원하는 주기로 자동 재생성하도록 예약합니다
  </Card>
</CardGroup>
