---
title: CLI
description: "CloudThinker CLI를 설치하고 터미널에서 로그인해 어떤 셸, 스크립트, CI 파이프라인에서도 에이전트를 실행하세요"
icon: terminal
---

<Note>
**미리 보기** — CloudThinker CLI는 얼리 액세스 단계입니다. 명령어 이름, 플래그, 설정 형식은 정식 출시 전에 변경될 수 있습니다. 미리 보기 참여를 원하시면 [문의하기](https://cloudthinker.io/contact)를 클릭하세요.
</Note>

`cloudthinker`는 워크스페이스를 터미널로 가져옵니다. CloudThinker 코딩 에이전트를 내 컴퓨터에서 실행하고, 어떤 스크립트에서든 Anna에게 프롬프트를 보낼 수 있습니다. 바이너리 하나면 되고 별도 런타임은 필요 없습니다.

## 할 수 있는 일

| 기능 | 설명 |
| --- | --- |
| 코딩 에이전트 실행 | `cloudthinker`는 현재 디렉터리에서 에이전트 세션을 시작합니다. 파일 편집과 명령 실행은 로컬에서, 모델과 [연결](/ko/guide/connections/overview)은 클라우드에 그대로 둡니다 |
| 헤드리스 프롬프트 전송 | `cloudthinker chat -p "..."`는 Anna에게 프롬프트 하나를 보내고 답변을 stdout으로 출력합니다. 바로 파이프할 수 있습니다 |
| 긴 실행 추적 | 기다리지 않고 제출하고, 최근 실행을 나열하고, 나중에 다른 컴퓨터에서 답변을 회수합니다 |
| 리뷰 확인 | 머지 리퀘스트나 풀 리퀘스트에 대한 [Review](/ko/guide/code-review/overview)의 상태, 판정, 발견 사항을 읽습니다 |
| 스크립트 인증 | 자체 자격 증명이 필요한 도구에 짧은 수명의 베어러 토큰을 전달합니다 |

## 설치

```bash
curl -fsSL https://cloudthinker.ai/install.sh | sh
```

설치 프로그램이 플랫폼을 감지해 `cloudthinker` 바이너리를 `~/.local/bin`에 두고 해당 디렉터리를 `PATH`에 추가합니다. 새 셸을 열고 확인하세요.

```bash
cloudthinker --version
```

macOS(Apple Silicon 및 Intel)와 Linux(x86_64 및 arm64)를 지원합니다. `cloudthinker update`로 언제든 업데이트할 수 있습니다.

## 첫 세션

<Steps>
  <Step title="브라우저로 로그인">
    ```bash
    cloudthinker login
    ```

    CLI가 동의 URL을 출력하고 브라우저로 엽니다. 요청을 승인하고, 계정에 워크스페이스가 여러 개면 하나를 고르면, 터미널이 저장한 워크스페이스를 알려줍니다.

    <Frame>
      <img src="/images/cli/login-consent-url.png" alt="동의 URL을 출력하는 cloudthinker login" />
    </Frame>
  </Step>

  <Step title="자신의 자격 증명 확인">
    ```bash
    cloudthinker whoami
    ```

    **성공 상태:** 호스트, 이메일, 활성 워크스페이스가 한 줄로 표시됩니다.
  </Step>

  <Step title="에이전트를 실행하거나 Anna에게 질문">
    ```bash
    cloudthinker                                   # 이 디렉터리에서 에이전트 세션
    cloudthinker chat -p "Check production health" # 헤드리스 프롬프트 한 번
    ```

    인자 없는 명령은 [로컬 코딩 에이전트](/ko/guide/cli/agent)를 실행합니다. 첫 실행에서 CLI 버전에 맞는 에이전트 빌드를 내려받습니다.
  </Step>
</Steps>

## 명령 목록

| 명령 | 목적 |
| --- | --- |
| `cloudthinker` | 현재 디렉터리에서 로컬 코딩 에이전트 실행 |
| `cloudthinker login` | 브라우저 또는 디바이스 코드로 로그인 |
| `cloudthinker logout` | 한 워크스페이스 또는 모든 워크스페이스의 자격 증명 삭제 |
| `cloudthinker whoami` | 저장된 자격 증명에 해당하는 실제 계정과 워크스페이스 표시 |
| `cloudthinker auth token` | 베어러가 필요한 도구를 위해 현재 액세스 토큰 출력 |
| `cloudthinker chat` | 헤드리스 프롬프트 전송, 실행 확인, 최근 실행 나열 |
| `cloudthinker review` | 머지 리퀘스트 URL로 추적 중인 리뷰 읽기 |
| `cloudthinker agent` | 로컬 코딩 에이전트를 실행하고 인자를 그대로 전달 |
| `cloudthinker update` | 최신 릴리스 설치 |

<Frame>
  <img src="/images/cli/command-surface.png" alt="모든 명령을 나열하는 cloudthinker --help" />
</Frame>

## 다음 단계

<CardGroup cols={2}>
  <Card title="인증" icon="key" href="/ko/guide/cli/authentication">
    브라우저 로그인, 다중 워크스페이스, CI용 토큰
  </Card>
  <Card title="로컬 코딩 에이전트" icon="robot" href="/ko/guide/cli/agent">
    내 저장소에서 에이전트 세션 실행
  </Card>
  <Card title="헤드리스 채팅" icon="terminal" href="/ko/guide/cli/headless-chat">
    Anna의 답변을 스크립트와 파이프라인으로 전달
  </Card>
  <Card title="레퍼런스" icon="book" href="/ko/guide/cli/reference">
    모든 명령, 플래그, 변수, 종료 코드
  </Card>
</CardGroup>
