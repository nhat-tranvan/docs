---
title: Local Coding Agent
description: "내 저장소 안에서 CloudThinker 에이전트 세션을 실행하고, 파일 편집은 로컬에서 모델과 연결은 클라우드에 두세요"
icon: robot
---

`cloudthinker agent`는 코드가 이미 있는 곳에서 코딩 에이전트를 실행합니다. 파일을 읽고 편집하고 명령을 실행하는 일은 내 컴퓨터에서, 모델과 워크스페이스 메모리, [연결](/ko/guide/connections/overview)은 CloudThinker에 그대로 남습니다.

## 로컬에서 실행하는 이유

- **업로드 없이 내 저장소 그대로.** 에이전트는 남의 샌드박스에 있는 사본이 아니라 현재 디렉터리에서 작업합니다.
- **내 셸 그대로.** 그 컴퓨터에 이미 있는 도구와 자격 증명으로, 사용자가 직접 칠 법한 명령을 실행합니다.
- **같은 워크스페이스.** 세션의 과금, 기억, 추론이 웹 앱과 동일한 워크스페이스에서 이루어집니다.

## 세션 시작하기

<Steps>
  <Step title="저장소로 이동">
    ```bash
    cd ~/code/payments-api
    ```
  </Step>

  <Step title="에이전트 실행">
    ```bash
    cloudthinker
    ```

    인자 없는 `cloudthinker`는 `cloudthinker agent`와 같습니다. 먼저 로그인 상태를 확인하고, 로그인이 없으면 브라우저 로그인을 안내하므로 새로 설치한 뒤에도 명령 하나로 세션에 들어갑니다.

    **성공 상태:** 현재 디렉터리에서 대화형 세션이 시작됩니다. 첫 실행에서는 해당 에이전트 빌드를 받는 동안 `downloading cloudthinker-agent <version>`이 표시됩니다.
  </Step>

  <Step title="원하는 변경 요청">
    파일 목록이 아니라 원하는 결과를 설명하세요. 에이전트가 스스로 저장소를 살펴보고, 수정안을 제시하고, 확인에 필요한 명령을 실행합니다.
  </Step>
</Steps>

## 단발 실행과 이어 하기

에이전트에는 자체 옵션이 있고, `agent` 뒤의 인자는 모두 그대로 전달됩니다.

```bash
cloudthinker agent -p "Add a health check endpoint to the API"   # 비대화형으로 실행 후 종료
cloudthinker agent -c                                            # 이전 세션 이어서
cloudthinker agent --model <pattern>                             # 이 세션의 모델 선택
cloudthinker agent -- --help                                     # 에이전트 자체 옵션 보기
```

CLI 자신의 옵션은 `agent` 앞에 옵니다.

```bash
cloudthinker --workspace Production agent
```

에이전트 옵션 이름이 CLI 옵션과 겹칠 때는 `--`를 쓰세요. 예를 들어 `cloudthinker agent -- --url http://localhost:3000`처럼 `--` 뒤의 모든 것은 에이전트의 몫입니다.

## 에이전트 빌드 설치 방식

에이전트는 CLI와 같은 릴리스에 번들로 들어 있으며, 따로 관리하는 다운로드가 아닙니다.

1. 설치 또는 업데이트 후 첫 `cloudthinker agent`가 해당 플랫폼용 번들을 내려받습니다.
2. CLI가 릴리스의 SHA-256 사이드카와 대조한 뒤에 압축을 풉니다.
3. `~/.cloudthinker/agent/bin/<version>/`에 설치하고 이후 실행에서 재사용합니다.
4. CLI를 업데이트하면 다음 실행에서 맞는 번들이 설치되고 이전 버전은 정리됩니다.

CLI와 에이전트 버전은 항상 함께 움직이므로 `cloudthinker update` 한 번이면 둘 다 최신이 됩니다.

## 지원 플랫폼

로컬 에이전트는 macOS(Apple Silicon 및 Intel)와 Linux(x86_64 및 arm64)에서 동작합니다. 그 외 플랫폼에서는 다른 것으로 대체하지 않고, 해당 플랫폼용 에이전트 빌드가 없다고 알려줍니다.

## 문제 해결

<AccordionGroup>
  <Accordion title="매번 다시 로그인하라고 합니다">
    저장된 자격 증명을 무언가가 덮어쓰고 있으며, 대개 셸에 남은 `CLOUDTHINKER_TOKEN`이 원인입니다. 변수를 해제하고 `cloudthinker login`을 한 번 실행하세요. [인증](/ko/guide/cli/authentication)을 참고하세요.
  </Accordion>

  <Accordion title="에이전트가 다른 워크스페이스에서 시작됩니다">
    에이전트는 CLI가 결정한 워크스페이스를 물려받습니다. `cloudthinker whoami`로 확인하고, `cloudthinker --workspace <name> agent`로 한 세션에만 고정할 수 있습니다.
  </Accordion>

  <Accordion title="CLOUDTHINKER_AGENT_BIN 경고가 표시됩니다">
    이 변수는 설치된 릴리스 대신 로컬 에이전트 빌드를 가리킵니다. 공식 번들로 돌아가려면 변수를 해제하세요.
  </Accordion>
</AccordionGroup>

## 관련 문서

<CardGroup cols={2}>
  <Card title="헤드리스 채팅" icon="terminal" href="/ko/guide/cli/headless-chat">
    스크립트에서 Anna에게 프롬프트 하나 보내기
  </Card>
  <Card title="레퍼런스" icon="book" href="/ko/guide/cli/reference">
    모든 명령, 플래그, 변수, 종료 코드
  </Card>
</CardGroup>
